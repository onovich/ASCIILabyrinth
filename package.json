{
  "name": "ascii-labyrinth",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-runtime.mjs && vite",
    "build": "node scripts/sync-runtime.mjs && vite build",
    "preview": "vite preview",
    "smoke": "node scripts/smoke.mjs",
    "visual-smoke": "node scripts/visual-smoke.mjs",
    "validate": "npm run build && npm run smoke",
    "verify": "npm run validate && npm run visual-smoke"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  }
}
